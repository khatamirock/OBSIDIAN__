# Done



# Learned
1. classic SQL "trapping" point! When you see that a staff member appears in both stores, a simple `WHERE store_id != 2` won't work because it ==only filters **rows**, not the **person**==. 
	1. (check 21 & 24 )
	2. To find someone who has **never** done something, you have to think in terms of **Exclusion** rather than just filtering.
	3. The "==List of Shame==" Approach (`NOT IN`) 
2.  most common mistake in ==LEFT JOIN== counting! Your query will report that an actor with ==**0** films actually has **1** film.==
	1. **The Mistake: COUNT(\*\)**
	2. COUNT(\*\) counts rows. It sees 1 row. Result = **1**, 
	3. ### Count a specific column
		1. Change COUNT(\*\) to **COUNT(fc.film_id)**.
3. 👉 **The columns in `DISTINCT ON (...)` must appear first in the `ORDER BY` clause.**
	1. see ex. 34 
	2. without it we will see incorrect results....
	3. 
4. an actor has 20 movies. You told SQL to smoosh them all into 1 row (GROUP BY actor_id)
	1. **Error:** column *title* must appear in GROUP BY or be aggregated
	2. **Why:** `GROUP BY actor_id` collapses many movies into 1 row, but SQL can’t guess **which title** to show
	3. 1. **ORDER BY a.actor_id, f.rental_rate DESC**: It sorts the whole list. For Actor #1, it puts their $4.99 movies at the very top.
	4. **DISTINCT ON (a.actor_id)**: It goes down the list. It sees Actor #1 (Row 1). It **keeps** it.
