


# DONE


# LEARNED
1. there is a diff of using group by and then aggregating !!
	1. 
	```sql
		SELECT
		customer_id,
		COUNT(*) AS rental_count
		FROM customer
		LEFT JOIN rental USING (customer_id)
		GROUP BY customer_id
		
		
		select count(*) as cnt, avg(length) as avl, rating from film group by rating
	```
	2. as we can see if we count rows and then need to find avg we need to use ==CTE==
	3. else we are fine with `avg` cause we need to find just the avg of the single column


### **The Dilemma:** [34 & 41....] problem
- If you GROUP BY category_id, you calculate the MAX(length) correctly, but SQL forgets which **Title** belongs to that length.
- If you GROUP BY title, you get every single movie back, and the MAX function becomes useless.

## `<>` is very very problematic !!
	cause it can cause mirroring and duplicate rows in between 2 tables......
		...........................
		 