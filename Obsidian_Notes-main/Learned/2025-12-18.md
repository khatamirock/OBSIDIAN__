# Learned Today
1. in grouping we need to use the field in the group or in the aggregation function...
2. ![[leftJoin_1.gif]]
3. ![[leftJoin_3.gif]]
4. #  🧠 Rule PostgreSQL enforces
> **Every selected column must be either**
> - in GROUP BY
> - OR wrapped in an aggregate function

5. Learned **Double Grouping Pattern"**.

# Todo
1. Finding What's Missing (The "Anti-Join")
2. The "Running Total" or "Time Series Analysis"
3. The "Conditional Bucketing" (Segmentation)
4. Greatest-N-Per-Group || Row with max value ( of that group )